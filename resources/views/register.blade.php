<x-layout title="Todo - Register">
    <x-slot:btn>
        <a href="{{ route('login') }}" class="btn btn-primary">
            Já possui conta ? Faça login
        </a>
    </x-slot:btn>

    Register

    <a href="{{ route('home') }}">Voltar para home</a>
</x-layout>
